{
  "name": "famora-workspace",
  "version": "1.0.0",
  "description": "Famora app workspace — powered by Supabase, no backend server needed",
  "scripts": {
    "dev": "node dev.js",
    "start": "node dev.js",
    "client": "npm run dev --prefix src/client",
    "build": "npm run build --prefix src/client"
  },
  "dependencies": {
    "expo": "^57.0.18"
  }
}
